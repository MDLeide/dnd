using System.Windows.Input;

namespace DND.UI.Components.IO
{
    class KeyBindings
    {
        public static KeyBinding ConfirmEditableTextBlock = new KeyBinding(Key.Enter);

        public static KeyBinding CancelEditableTextBlock = new KeyBinding(Key.Escape);
    }
}
